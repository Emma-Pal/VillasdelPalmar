node_modules/
.env

# Base de datos local (versión Node, ya no se usa en producción) y archivos
# subidos: contienen datos de propietarios y nunca deben subirse al repositorio.
data/
uploads/

# Credenciales reales de MySQL (versión PHP) — solo existe en el servidor,
# se sube app/config.example.php como plantilla en su lugar.
app/config.php
